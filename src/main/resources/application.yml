server:
  port: 8000

  spring:
    neo4j:
      uri: neo4j://localhost:7687
      authentication:
        username: neo4j
        password: graph

logging:
  level:
    org:
      springframework:
        data:
          neo4j: DEBUG

  spring:
    data:
      neo4j:
        database: neo4j